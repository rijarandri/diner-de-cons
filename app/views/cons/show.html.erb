<h1>C'est la show du CON</h1>

<h1><%= @con.name %></h1>
<p>
<p>
  <strong>Catégorie :</strong>
  <%= @con.category %>
  <% @con.bookings.each do |booking| %>
    <%= booking.date %>
  <% end %>

  <p><strong>Description :</strong>
  <%= @con.description %>
</p>
</p>
  <strong>Prix :</strong>
  <%= @con.price %>
</p>

<%= link_to "Book this con for your party", new_con_booking_path(@con) %>


<%= link_to "Back to home page", root_path %>
