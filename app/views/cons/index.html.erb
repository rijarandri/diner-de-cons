<h1>Hello c'est la liste des cons</h1>


<% @cons.each do |con| %>
 <%= link_to con.name, con_path(con) %>
 <% if con.photos.attached? %>
<% con.photos.each do |photo| %>
  <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
  <% end %>
<% end %>
 <p><strong>Catégorie:</strong> <%= con.category %></p>
 <p><strong>Description :</strong> <%= con.description %></p>
 <p><strong>Prix:</strong> <%= con.price %></p>
<% end %>
